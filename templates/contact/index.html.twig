{% extends 'base.html.twig' %}

{% block content %}
{#
    <div class="jumbotron txt-center">
        <h1>Contact</h1>
    </div>
    <div class="container">
        {{ form_start(form) }}
            {{form_widget(form)}}
            <button type="submit" class="btn btn-success">Envoyer</button>
        {{ form_end(form) }}


    </div>
#}
    <div class="jumbotron txt-center">
        <h1>Contact</h1>
    </div>
    <div class="container">
        <h1 class="display-3">Indisponible</h1>
    </div>
{% endblock %}