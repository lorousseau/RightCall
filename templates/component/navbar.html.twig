{# barre de navigation top #}
<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <a class="navbar-brand" href="{{ path('home') }}">
        <img src="{{ asset('images/logo_angers.jpg') }}" alt="logo_angers" class="img-fluid"   <img src="{{ asset('images/logo_angers.jpg') }}" alt="logo_angers" class="img-fluid" width="60px"/>
    </a>
    <div class="collapse navbar-collapse">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('home') }}">RightCall<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Site
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('emerald') }}">Emerald</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="#">Elsevier (indisponible)</a>
                    <a class="dropdown-item" href="#">Taylor & Francis (indisponible)</a>
                    <a class="dropdown-item" href="#">Springer (indisponible)</a>
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                   Classements des revues
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('classementCnrs') }}">CNRS</a>
                    <a class="dropdown-item" href="{{ path('classementFnege') }}">FNEGE</a>
                    <a class="dropdown-item" href="{{ path('classementHceres') }}">HCERES</a>
                </div>
            </li>
        </ul>
        {% if not app.user %}
            <div class="justify-content-end">
                <a class="btn btn-outline-light" href="{{ path('security_login') }}">Connexion</a>
            </div>
        {% else %}
            <div class="justify-content-end">
                <a class="btn btn-outline-light" href="{{ path('security_logout') }}">Déconnexion</a>
            </div>
        {% endif %}

        <a class="btn btn-outline-light" href="{{ path('inscription') }}">Inscription</a>
    </div>
</nav>
{# fin barre de navigation top #}